<UserControl
    x:Class="WPF.BehaviorDemo.Views.EditOptionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF.BehaviorDemo.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="White">
        <DockPanel>
            <Button
                Margin="5"
                Content="应用"
                Command="{Binding ViewOptionsChangedCommand}"
                CommandParameter="{Binding ViewOptions}"
                DockPanel.Dock="Bottom" />
            <toolkit:PropertyGrid
                MinWidth="300"
                MinHeight="400"
                SelectedObject="{Binding ViewOptions}" />
        </DockPanel>
    </Grid>
</UserControl>
